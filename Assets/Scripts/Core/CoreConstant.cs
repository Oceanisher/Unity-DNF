using UnityEngine;

namespace Core
{
    //通用常量
    public class CoreConstant
    {
        //所有精灵通用的偏移量
        public static readonly Vector2 DefaultOffset = new Vector2(232, 333);
        //连续按键间隔时间，毫秒
        public static readonly int SustainKey = 50;
    }
}